apiVersion: v1
kind: Service
metadata:
  name: catalogue
  namespace: roboshop
  labels:
    project: roboshop
    env: dev
spec:
  selector:
    project: roboshop
    component: catalogue
    env: dev
    tier: app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080